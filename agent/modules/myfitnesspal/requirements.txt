# Base (required by all modules)
fastapi>=0.109
uvicorn[standard]>=0.27
structlog>=24.1
pydantic>=2.5
pydantic-settings>=2.1

# Module-specific
# NOTE: myfitnesspal is installed --no-deps in Dockerfile to avoid
# rich version conflict with seleniumbase (rich<13 vs rich>=14).
seleniumbase>=4.32
requests>=2.31
