# Nexus Portal Frontend Environment Variables

# Application environment (affects environment badge display)
# Options: "development" | "staging" | "production"
# Default: Automatically determined by build mode
# VITE_APP_ENV=staging
